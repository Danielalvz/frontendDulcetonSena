http {
    include    $!{nginx}/conf/mime.types;
    index    index.html index.htm index.php;
    server {
        listen ${PORT};
        listen [::]:${PORT};
        server_name localhost;
	
		location / {
			root /frontend;
			index index.html;
            try_files $uri $uri/ /index.html =404 index.html;
		}
		location /frontend {
			try_files $uri $uri/ /index.html index.html0;
			index index.html;
		}
	}
}